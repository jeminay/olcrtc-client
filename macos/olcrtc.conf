# olcRTC tunnel config for macOS
# ROOM_ID changes every server restart. Get it from server log.
ROOM_ID=
KEY=
SOCKS_HOST=127.0.0.1
SOCKS_PORT=8808
DNS=1.1.1.1:53

# Routing config for sing-box TUN
DIRECT_PROCESSES=olcrtc,sing-box
DIRECT_DOMAIN_SUFFIXES=wb.ru
DIRECT_IPS=YOUR_VPS_IP/32
PRIVATE_DIRECT=true
